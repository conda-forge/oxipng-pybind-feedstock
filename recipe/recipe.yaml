schema_version: 1

context:
  version: "10.2.0.post1"

package:
  name: oxipng-pybind
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/o/oxipng-pybind/oxipng_pybind-${{ version }}.tar.gz
  sha256: 15734a9aa03e89217c498b9e8bcd6415b2a6b7f0116122cedbece25eb8f64355

build:
  number: 0
  script:
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - ${{ stdlib("c") }}
    - ${{ compiler("c") }}
    - ${{ compiler("rust") }}
    - cargo-bundle-licenses
  host:
    - python
    - maturin >=1.8,<2.0
    - pip
  run:
    - python

tests:
  - python:
      imports:
        - oxipng
      pip_check: true

about:
  summary: Focused Python wrapper for oxipng.
  license: Unlicense
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  homepage: https://github.com/pdomain/oxipng-pybind

extra:
  recipe-maintainers:
    - thewchan
